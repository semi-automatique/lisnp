============
Installation
============

At the command line::

    $ easy_install lisnp_python_package

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv lisnp_python_package
    $ pip install lisnp_python_package
